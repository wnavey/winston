# Winston Scratch-Pad

Running index of Will's most recent **15** specs / winston work — newest at the top.
This is the fast lookup so you don't have to hunt for full paths. When a new spec is
created (or an existing one materially revised), it goes to **row 1** and everything
below bumps down; the 16th-oldest row drops off.

**Columns:** `#` (1 = most recent) · what it was · path to the spec that was
added/modified · the date stamped on that spec · when it was engaged / merged to main.

> Maintenance: this file is kept current by the `winston-spec` skill. Whenever you add
> or revise a winston spec, prepend a row here and trim the list back to 15. Paths are
> relative to the winston repo root.

| # | What | Spec file | Spec updated | Engaged / merged |
|---|------|-----------|--------------|------------------|
| 1 | **Seat resolution — spending the right person's subscription account on a cloud run** (Draft **v4**, split from cloud-captain §6): a cloud subscription run spends substation's **single shared `CLAUDE_CODE_OAUTH_TOKEN`**, which `docs/runbooks.md:74` ("Subscription accounts are never shared") and `:76` ("only the accounts owned by the user requesting the run") both forbid. Latent until **bureau#1676 merged** — the rule now says **cloud runs also default to subscription**, so the documented default is now AHEAD of the implementation. Research: `dsd.conf` is a **per-machine dotfile** ("NOT checked in", `account = <alias> <config_dir>` + `owner = <name> <accounts…>`, so alias and ownership already exist); **a seat is a `CLAUDE_CONFIG_DIR`, not a token**; `dsd seat pick` is a **stdout hook contract** (a path / `-` / empty; exit 0 picked, **75 parks the step**) with precedence `--seat` → preset `seat:` → hook, and per-machine leases + usage snapshots. None of it ports to a sandbox — but conductor's **seam** does, and this implements behind it rather than replacing it. D1 service-role-only registry keyed by alias+owner, published from `dsd.conf` by a `dsd` command (no read policy); D2 resolution in **substation at launch**, before a sandbox exists, so a billing problem costs nothing; D3 optional `seat` alias on `POST /api/runs` → `CONDUCTOR_SEAT`, beating the `triggered_by` lookup; D4 nothing resolves ⇒ an `operator` gate offering metered or cancel, never a failed launch; D5 **ownership enforced, not documented** — refuse an alias not owned by `triggered_by`; D6 leases + usage-aware picking are phase 2. **Q1 gates implementation**: a local seat is a logged-in config dir, so where a per-account cloud token comes from is unanswered. Interim = cloud-captain **D10** (caller supplies their own token per run). **v2 (naming only, no decisions changed):** the registry table is named **`subscription_seat`** — v1 described it four times and never named it, one inference away from `runbook_run_secret`, which is a *different* already-shipped table (captain D10, migration `20260921200000`, one row per run, deleted at terminal status); no `runbook_` prefix because the registry outlives any run. Its `credential` column is renamed **`claude_code_oauth_token`**, matching `runbook_run_secret.claude_code_oauth_token` and the env var it becomes, so nobody later puts a `config_dir` or a refresh token in it. Q1 still open. **v3 adds D7 — the run records who launched it:** D2 and D5 both resolve against `runbook_runs.triggered_by`, which is **NULL on all 26 rows in prod** because `POST /api/runs` never writes it. The mechanism is already shipped — `authMiddleware` resolves `x-on-behalf-of` into `user.id`, and the HITL answer route already uses it for attribution via a `SERVICE_SENTINELS` set — so D7 is one header from `conductor init` plus one column on the insert. Stated honestly as **attribution, not authorization**: the caller holds the service-role key, so the identity is self-asserted and D5 is mistake-prevention. **Q6 (new) — how a launch proves who it is** (verified JWT, or a per-operator key replacing the shared service-role key) is the next thing to settle, and it also decides how §3.1's rows get written. **v4 answers Q6 with D8 — a per-user substation API key, `SUBSTATION_PERSONAL_API_KEY`**, required for a **cloud subscription** launch only (metered keeps the shared `SUBSTATION_SERVICE_API_KEY`; local is untouched because substation is not the one spending — the seat is a directory on the operator's machine). No expiry, revocable, `last_used_at`; minted self-service in cityhall under the person's own SSO session, stored hashed in a new `personal_api_key` table; lives in the operator's `~/.env`, **never in substation's environment** — substation holds a table, the laptop holds the key, so adding a person is a row not a redeploy. Needed on the **launch call alone**; the sandbox uses its per-run bearer, so the personal key never enters the cloud. **The refusal is the control:** cloud + subscription + a shared credential ⇒ 403, else D8 is advisory. Correction folded in: scoped this way it does NOT retire conductor-init D4's service-role debt. **Q7 (new) — who writes the `subscription_seat` rows**: `dsd.conf` holds no token at all, so D1's publish can project alias+owner but never the credential (`claude setup-token`, interactive, per account), and `will`/`jason` still need bridging to `auth.users` UUIDs. | `workspaces/runbook-checkpoints/seat-resolution/DESIGN-SPEC.md` | 2026-09-22 | 2026-09-22 (#272) |
| 2 | **Cloud Captain — an Agent SDK captain inside the Vercel Sandbox** (Draft **v3**, grilled 2026-09-21): builds the captain `bureau/docs/runbooks.md:12,40,42` has always specified for cloud runs and nobody wrote — the image already installs `claude-agent-sdk==0.2.144`, but substation shells `conductor advance` bare, so a cloud run is a binary with a cron around it. **Corrects `agentic-hitl` §3** ("no agent in the loop at all" is an unbuilt component, not architecture). Evidence: run `152db1b9` rendered a HITL card with **no question** while `payload.readout.text` held the whole digest unread; `3b601464` **died** on a plain-English refusal no checkpoint carried. **D1 park-and-exit** — the captain is the carrier the reconcile loop launches (one line at `reconcile-loop.ts:416`), so a run parked for weeks costs storage, not a live 4-vCPU box. **v2 corrects its own §4 premise**: the sandbox *does* hold `SUPABASE_RUN_TOKEN`/anon/OAuth; the real reason for the two new `/api/runs/:id/gates` bearer routes is that `runbook_hitl_ask` is `EXECUTE`-granted to **service_role only** (#267) and all three tables are **SELECT-only RLS, no write policy** — verified in prod. **D8 split repair authority**: env fixes, re-runs and void/REVISE free; `contract.test.ts`/`prompt.md`/`step.yaml` edits need a HITL gate **whose readout is the diff**, so every cloud runbook edit carries a human signature. **D5 loop-count cap (3), not dollars**, and exhaustion **parks on an operator gate rather than failing**. **D9 every captain question is a DB row on BOTH lanes** (local answers may still come via chat as `channel='session'`) — puts claude-plugins in scope. **D10 per-run `claude_code_oauth_token` on the launch body**, never in `request` (row-level RLS ⇒ team-readable plaintext), stored write-only in `runbook_run_secret` and injected each pass — because park-and-exit rebuilds sandbox env on resume, so a token supplied once can't survive its own gate. D11 expiry → operator gate; D12 no token ⇒ today's shared-token behaviour (purely additive); D13 seat exhaustion parks, never auto-meters. **D7 always on, no flag**; D14 the captain narrates itself; D15 minimal completion validation. **D6 subscription by default** — local now, cloud when a seat resolves; needs a **human** edit to `docs/runbooks.md:85`. **v3: §6 seat resolution split into its own spec** (sibling row above; v2 D16/D17 moved there, D1–D15 unchanged), and **bureau#1676 merged** so `docs/runbooks.md` now says *cloud runs also default to subscription* — D6 implements the constitution instead of contradicting it, and the dead `:40`/`:42` citations are re-pointed at `:10,12`. Depends on R1: the park path **never stops the sandbox** (`sbx_Fvw0…` ran through a 28-min park), a regression from #257. Q1–Q7. | `workspaces/runbook-checkpoints/cloud-captain/DESIGN-SPEC.md` | 2026-09-21 | 2026-09-21 (#271) |
| 3 | **Cartographer — placing extracted figures relative to each other** (Draft v1): new `extract-geometry` placement phase so a file's figures stop stacking at `(0,0)`. Extraction stays unanchored; a vision worker claims relationships (shared lines, P.O.C. ties), zero-LLM `place.ts` computes offsets and residuals, and connected components of evidence become **frames**, so unrelated shapes in one document stay separate. Precedence shared line > tie > printed coords; no new HITL stop (reported in the readout); translation only; additive `frame_key`/`offset_x/y`/`placement_*` columns; `/processed` draws one frame at a time. Acceptance = car-wash plat re-run. Q1–Q5 resolved same day: one foot-based position tolerance (accept ≤0.5 ft, check ≤1.0 ft), worker-only links, largest figure = reference, one worker per document, top-of-readout plat-defect flag. | `workspaces/cartographer/geometry-placement/DESIGN-SPEC.md` | 2026-09-21 | 2026-09-21 (#270) |
| 4 | **Agentic HITL — the agent's inputs and outputs at a runbook gate** (Draft v1, as-built) + `agentic-hitl.html` companion (6 tabs: the gate · the agent's I/O · local lane · cloud lane · where the schema is · what to cut). Answers the question winston#259 left open: **what does the agent actually do at a gate?** Three corrections. (1) **No agent runs at a gate** — `runner: none` launches no harness, calls no model, spends no tokens; the step's contract just fails until `decision.json` exists and the pass exits 75. The agency belongs to the DRIVER. (2) The driver is an agent on **only one lane**: locally the `/conductor` captain publishes the gate (`ask`) and writes the decision (`await-answer`); in the cloud conductor2's binary reports it over `--callback` and the reconcile loop writes the file — **no agent in the loop at all**. (3) The captain's entire authorship at a gate is the `--prompt` one-liner; `kind`/`readout`/`allow_report_back` are structural, and it **never reads, chooses or validates against `decision.schema.json`** — it learns the shape from `adjudication.md` prose + the zod in `contract.test.ts`, like a human. Its one decision power is `directives.decide` (a step-id allowlist in `request.json`), honored purely by instruction-following — no code reads it. **The schema gap, verified in prod:** `schema_for` (callback.rs:141) is reached only from the cloud callback (`verbs.rs:2554` requires `--callback` + `--run-id`); the local lane registers via `runbook_hitl_cli.py ask`, whose `--schema-file` flag **nothing ever passes** → 11 gate rows all time, **1** with a schema, and that one still `open`. The ajv arm has never validated a real answer, and `smoke` vs `smoke-schemaless-hitl` (bureau#1623) are behaviorally identical. Cut order: ①`decided_by` from SSO (SHIPPED substation#265 + cityhall#678) → ②retire the `choice` vocabulary → ③close the local-lane gap → ④then pick a floor. Q1–Q5 (schema as validation vs rendering, `ask` reading the file itself, filename-inferred `gate_kind`, enforcing `directives.decide`, local-only self-decision). **+ `gate-loop.html` (#268)** — the one-page short version, on request: a single tab, a single left→right flow of the 2.3-hitl gate (readout → ask → card → ajv → poll → decision.json → contract), with the step strip 2.1/2.2/2.3/2.4 above it and a five-row table answering who asks, how we wait, what validates, where the schema is, and what actually holds (the zod, not the schema). Read this one first; #267 is the detail. | `workspaces/runbook-checkpoints/agentic-hitl/DESIGN-SPEC.md` + `gate-loop.html` | 2026-09-18 | 2026-09-18 (#267, #268) |
| 5 | **Cartographer geometry-extraction runbook** (`extract-geometry`, Draft v1): an agentic runbook living IN cartographer (not bureau) that takes a `cartographer_files.id`, reads a recorded plat, and lands renderable **SRID:0** geometry in the DB — the missing link between the file intake ([[cartographer-app-spec]] winston#265) and the SVG canvas. Shaped on `bureau/runbooks/preprocessing-v3` (top-level runner → background orchestrator → per-page/per-region workers → one HITL stop → dumb publisher; folder contract = API + resume + audit). Five phases: kickoff (fetch+`pdftoppm`) → **survey pass** (Opus vision, 1/page: which pages, what kind, WHERE — regions, no values) → **transcribe pass** (Opus vision, 1/region, `magick` 300-DPI crops: `verbatim` string + structured `{quadrant,d,m,s}`+distance) → **assemble** (ZERO LLM) → HITL ⛔ → publish. **The deterministic half already exists** — `Bearing`/`Course`/`runTraverse`/`traverseMisclosure`/`meteToPolygon`/`parseMete`, 62 tests — so `scripts/assemble.ts` imports `src/lib/geometry` directly (D6: coupling is the point, a second copy would drift from the renderer's). **Honesty boundary:** the agent only transcribes; every number in the DB comes from the geometry lib, so a misread shows up as a wrong shape, not a plausible number. **Verbatim diff** (`formatBearing` re-render vs the printed string) is the cheapest quality gate. New `cartographer_geometries` table stores BOTH `mete` (source description) and `polygon` (derived drawing) so a traverse-engine fix can re-derive without re-reading PDFs; no run registry — republish deletes (D5). Validation (misclosure · area vs stated · verbatim diff) is **reported, never blocking** (D4) — open figures draw as unfilled polylines. New `/processed` UI: file rail left, `SvgCanvas` right, per-figure toggles. Deferred: curves (chord approximation, `isChord` flag), anchoring/WGS84, closure enforcement, versioned runs, coordinate-table-primary reconstruction. Built against the **2024178771 car-wash plat** — the same document [[3089-spec]] reconstructed by hand, so run 1 has a known-good answer. Q1–Q5 (region granularity, crop DPI vs the navalbase ~44-DPI misread, multi-figure attribution, label semantics, show-the-crop). | `workspaces/cartographer/geometry-extraction/DESIGN-SPEC.md` | 2026-09-18 | 2026-09-18 (#266) |
| 6 | **Sandbox API tokens — a per-run bearer for a subset of substation routes** (Draft v1): give conductor2 a per-run credential to `GET`/`POST` a fixed set of `/api/runs/:id/*` routes, so the **harness itself** (not an LLM shelling out to `runbook_hitl_cli.py`) writes each step's checkpoint — identically local + cloud — without ever holding a credential whose leak outlives the run. **Not a new pattern:** the union of two substation already has — the per-run HMAC bearer (`run-bearer.ts`, `HMAC(run_id)` keyed by `RUN_CALLBACK_SECRET`, derived-not-stored, already gating callback+decide, already minted at `POST /api/runs` and stored by `conductor init` at `.conductor/callback-token`) checked against a route allowlist (`auth.ts` `SERVICE_API_ROUTES` shape). Security spine is intrinsic: auth = `verifyRunBearer(:id-from-path, token)` → a token can only touch its own run's sub-resources, cross-run is cryptographically impossible. New artifact = one reusable `runBearerMiddleware` + `RUN_BEARER_ROUTES` (all under `/api/runs/:id/*` so `HMAC(:id)===token`); composite auth (run-bearer OR service-role OR user-JWT) keeps console reads + reconcile writes. conductor2 gets a `control_plane` ureq client (stays DB-free) + a best-effort checkpoint emit teed off `ledger::append` right next to the existing `telemetry::emit("ledger",…)` — one choke point, both lanes, every step. Cloud token delivery via `buildRunbookEnv` injecting `CONDUCTOR_CALLBACK_TOKEN` (mirrors `SUPABASE_RUN_TOKEN`; secret never enters the sandbox). Implements conductor-init **D5**. Ships the one-way slice first (GET run, POST checkpoints, callback migration, status emit); HITL `ask`/`await` routes deferred (cloud resume needs the unbuilt reconcile body). **Grilled 2026-09-11** → decisions D1–D8: node-type partition (conductor2 = `status` for executed nodes; captain keeps gate `question`/`note` + `runbook_hitl_question` until the cloud reconcile body unifies both lanes), server-assigned `seq` + additive `runbook_checkpoints.client_token` idempotency amended into the still-open reconcile migration, enter+exit emit teed off `ledger::append`, one no-expiry bearer with **bounded authority** (GET projection only), status-only first slice. Open Q1–Q6 (composite-auth mechanics, GET-projection fields, backfill/lossy-stream, secret-rotation ops, token delivery + local file→env bridge, writer-partition test). | `workspaces/runbook-checkpoints/sandbox-api-tokens/DESIGN-SPEC.md` | 2026-09-11 | 2026-09-11 (#258) |
| 7 | **`conductor init` — one local run-initialization verb** (Draft v1): give the LOCAL lane a single verb mirroring what the cloud lane already does (substation#257 retired the Inngest launcher — `POST /api/runs` is the sole cloud entry + the reconcile `setup` carrier runs `conductor setup`). `conductor init <runbook> <run-dir> [--run-id]` = `[POST /api/runs unless --run-id, via ureq + service-role key — conductor stays DB-free] → conductor setup → write runbook-run-id.txt`. **Retires `CONDUCTOR_HITL_DB=1`**; the id file's presence becomes the durable per-run lane signal (the env var didn't survive the captain's separate tool calls). substation refactors `POST /api/runs` to share `registerRun()`; the `/conductor` skill §3 calls `init` not `setup`; `setup` stays public as the offline no-DB primitive. **Cloud untouched** (register + setup are deliberately decoupled across the reconcile tick — a cold sandbox sits between them). Deferred: checkpoints/ask-await off the LLM, the `--callback` binary contract, retiring `runbook_hitl_cli.py`. Decisions D1–D10 locked (grill 2026-09-10): idempotency (marker-check first), host≠vercel guard, delete `run-start`, service-role-key debt accepted, ship init-only, POST-fails→degrade, register vs prod, `setup` stays public, name `init`, bearer stored-optional. | `workspaces/runbook-checkpoints/conductor-init/DESIGN-SPEC.md` | 2026-09-10 | 2026-09-10 (#255) |
| 8 | **Reconcile the two Runbook Control Plane workstreams** (Draft v3) + `reconcile-diagram.html` companion (4 tabs: summary · code deltas · data deltas · path forward). Jason's `runs`+`gates` (**substation#247**, MERGED `d1c082b`) collided with my `runbook_run`/`runbook_checkpoint`/`runbook_hitl_question` + local lane + console (#245/#1549/#663, open) — first a migration-filename clash (fixed by renaming mine to `20260909000000`), then architecture. **Venn of deltas:** *A-only* = launch/callback/reconcile-stub routes + per-run HMAC bearer + ajv-vs-`gates.schema` + sandbox liveness + test suite; *B-only* = checkpoint stream table + `clarification` kind + live RLS/realtime + bureau LOCAL captain CLI + cityhall console UI + `answered_by`(FK)/`channel`; *overlap* = the decide route built twice (`/api/gates/:id/decide` ajv vs `/api/runbook-hitl-questions/:id/answer` zod), the run+gate tables (renamed), and conflicting enum literals (`parked` vs `awaiting_input`, `open/decided` vs `awaiting_input/answered`). Neither side has a live reconcile loop. **Plan: converge on A's merged base, graft B's additive surface** — #245 mostly dated (chunk competing tables + answer route; re-cut `checkpoints`+`clarification`+RLS/realtime onto `runs`/`gates`), #1549+#663 keep & re-target names. Q1–Q6 (A-base, extra statuses, checkpoint FK, run_dir derive-only, schema-on-gate, lease-vs-probe). **v2 (Will feedback):** rename `runs`→`runbook_runs` / `gates`→`runbook_hitl_questions`; decide = two routes over one controller (keep `/api/gates/:id/decide` alias + add `/api/runbook-hitl-questions/:id/answer`); keep A's `schema` (validation) AND add B's `payload` (render) — orthogonal; add `cancelled` + `clarification`; drop `run_dir` (derive-only). **v3:** two-part HITL — every question gains a human **next-action directive** at answer time (Proceed = move forward vs Report back = ingest + take next step, then pause & report); new `next_action` column on `runbook_hitl_questions`, local captain honors it (locally-testable MVP target). | `workspaces/runbook-checkpoints/reconcile-workstreams/DESIGN-SPEC.md` | 2026-09-09 | 2026-09-09 |
| 9 | **Runbook Checkpoints + HITL + cityhall UI** DESIGN SPEC (Draft v1) + `data-model-spec.html` companion: UI-answerable HITL for all runbooks (1.0 + 2.0) via **3 new substation tables** — `runbook_run` (lifecycle; `awaiting_input` status; `runbook_name`/`project_id`), `runbook_checkpoint` (ONE ordered stream drives the UI; `kind` status|note|question + FK to the question), `runbook_hitl_question` (**options-in-payload generated from the gate's zod contract**; `input_type` decision|clarification supertype; race-safe answer + `channel` ui|session|agent). Two lanes share the tables: **local** captain shells out to `app_client.py` + polls (answerable in-session OR UI); **cloud** launcher emits + exports readout to Storage + resumes via an Inngest event that writes `decision.json` (UI-only). Verified in conductor2: exit-75 resume is **token-free** (fresh per-node agents, no conversation replay) → always-exit-75; multi-turn on both lanes via an out-of-band `conductor run-step` clarifier (**zero conductor2 change**). cityhall internal console gated to the **noetic** org (`isNoetic`). Also relocates the HITL catalog under `runbook-checkpoints/HITL/`. Open Q1–Q6 (step-plan storage, scratch/ persistence, run-dir locking, shared-schema pkg, cloud wait-model sign-off, inline-1.0 checkpoints). **Impl shipped as 3 PRs** — substation#245 (migration + zod + answer route), bureau#1549 (local captain CLI), cityhall#663 (runbook-runs console). **Companion HTML §5 "Cloud routes" added (2026-09-09):** per-route boxes-and-arrows for the 4 control-plane routes (`POST /api/runs` launch · `POST /api/runs/:id/callback` park→gate-rows · `POST /api/gates/:id/decide` answer · `GET /api/cron/reconcile` backstop) — clarifies how & when each fires (3 are Phase-2 / unbuilt, decide ships in #245). | `workspaces/runbook-checkpoints/data-model-spec.html` | 2026-09-08 | 2026-09-09 |
| 10 | **Runbook HITL Questions — Catalog** (Draft v2 — **all 12 bureau runbooks now mapped**): cross-runbook index of every human-in-the-loop question each runbook asks the operator. v2 introduces the **Runbook 1.0 vs 2.0** taxonomy: **1.0** = prose `RUNBOOK.md` run inline in the local CC HITL workspace, fanning out to headless sub-agents → HITL collapses into pre-flight ⛔ (`PF-`) + readout ⛔ (`RO-`) + suppressed skill HITL 🔕 (`SK-`); **2.0** = graph/conductor runbooks (`runbook.yaml` + `steps/*/step.yaml`) where a human gate is a **`runner: none`** step (conductor exits 75, parks for a human file-drop), plus contract-failure/`NEEDS-OPERATOR` sentinels and the **`directives.decide[]`** headless-authority escape hatch. 1.0: `review`, `sir`, `preprocessing`, `preprocessing-v3`, `process-city-response-docs`. 2.0: `review-new`, `sir-new`, `guide-training`, `site-research`, `jurisdiction`, `smoke`(+`smoke-package`=0 gates). Key findings: **no runbook uses `AskUserQuestion`**; 2.0 skeleton is uniform (`1.1-inputs` request gate + `…-deliver` approve/publish gate); publish is always the last, most-guarded gate. Open Q1–Q6 (suppressed-HITL scope, winston-vs-bureau home, shared vocab, superseded-runbook upkeep, jurisdiction contract-as-gate pattern, CC/CRC workflows out of scope). | `workspaces/runbook-checkpoints/HITL/runbook-hitl-questions.md` | 2026-09-08 | 2026-09-08 |
| 11 | **Process City Response Documents** RUNBOOK DESIGN SPEC (Draft v1): new `bureau/runbooks/process-city-response-docs/` (RUNBOOK.md prose-orchestrator, sibling of the deferred processing side of city-response-docs) that takes `projectId`/name + `submission_version.version_number` (implicit single `type='site_plan'` submission, city-submitted version), discovers attached `document.kind='city_response'` docs via `document_version.submission_version_id`, and allocates one processing job per doc. Routes by `city_response_type`: `mcr` → `generate-crc-guides`; `redlines` → navalbase `step-3-analyze-pdfs` → `refine-step-3-output` → `generate-crc-guides-from-redlines` (no `layer-2-enrich`); `misc` → skip. **Invoke the CRC skills in place** (canonical home stays claude-plugins); harness sub-agents can't do live HITL → **pre-flight batches ALL human decisions** (unknown MCR dept-prefix naming + pre-INSERT `jurisdiction_departments`, versioning=bump, per-redline dept code/label) then headless parallel workers + one end readout gate; emergent HITL → keep-and-flag (`--defer-uncertain`). Mirrors the pre-processing registry: new `city_response_processing_run` table (grain=submission_version, active/inactive, one-active index) + nullable `document_version.city_response_processing_run_id` FK stamp = processed badge. Publish is a **thin FK-stamp** (guides already in `crc-guides` bucket); generation numbering stays CRC's source of truth (registry = provenance/badge only, D19). substation migration → bureau+claude-plugins flags → cityhall badge (deferred). Open: Q1 nested-spawn depth, Q2 refine cost, Q3 runbook_ref, Q4 gen source-of-truth, Q5 jurisdiction resolve. | `workspaces/city-response-docs/preprocessing/DESIGN-SPEC.md` | 2026-09-04 | 2026-09-04 |
| 12 | **City Response Documents** DESIGN SPEC (Draft v1): attach/view/CRC-fetch the city's response files (MCR, dept redlines, misc) against a **city-submitted** submission version (gated `city_submission_number IS NOT NULL`). Reuse `document` (`kind='city_response'`) + exactly **one** `document_version` as file-carrier (no sections, no `submission_document` junction, no Inngest, no version history — response-to-U0 vs -U1 are separate docs); new nullable `document.city_response_type` (`mcr`/`redlines`/`misc`, user-picked at upload). Reuse `submission-data` bucket at `{projectId}/city-response/{submissionVersionId}/{documentId}/{filename}`; new `commit-city-response` endpoint (explicit `submission_version_id` in body, reuses `prepare-upload`). UI: gated "+ Add City Response Files" footer button + section above Plan Set + `kind`-gated simplified doc viewer (PDF inline / TXT `<pre>` / DOCX download). CRC gets a documented direct-Supabase fetch contract (routes by sub-type); skill rewire + ZIP/auto-type = deferred. substation before cityhall. Open: Q-A processing_state sentinel, Q-B kind-absent query sweep, Q-C coherence CHECK, Q-E DOCX→CRC. | `workspaces/city-response-docs/DESIGN-SPEC.md` | 2026-09-03 | 2026-09-03 |
| 13 | Pre-Processing v2 **comparing preprocessed outputs (v1 vs v2)** WORKFLOW SPEC (Draft v1): read-only eval harness that compares the same submission (1700 S Lamar sub v4) processed the **old** way (`23301a8a…`) vs the **v2 runbook** (`ed9e7ec4…`), both staged via `review-new` 1.1/1.2 under `working/review-new/<proj>/1.2-stage-submission`. Deterministic alignment pre-pass (artifact parity, sheet-by-number w/ identical raster sha256, **14↔14 doc crosswalk** since slugs differ) + preflight completeness guard (staging `_health.json` false-greens empty guides), then a Claude Code **Workflow** fan-out — 1 Opus agent per sheet (or per ~5) + 1 per supplementary doc — each scoring transcription-fidelity / semantic-accuracy / coverage and adjudicating stark diffs against the byte-identical `sheet-NN.jpg` / `source.pdf`. Output = multi-tab **inspector-general** report (overall parity · plan-sets · one tab per doc). Appendix records the 2026-09-01 incomplete-v2 fallback (source sheets from `phase-1-reading/reconciled.json`). Open: Q1 fan-out granularity, Q2 verify-stage, Q3 score rollup, Q4 doc provenance. | `workspaces/pre-processing-v2/comparing-preprocessed-outputs/WORKFLOW-SPEC.md` | 2026-09-01 | 2026-09-01 |
| 14 | Pre-Processing v2 **file-upload jobs — streamed loading UX + HITL version/replace/separate/discard** DESIGN SPEC (**de-versioned** — revision-note scaffolding stripped, reads as one current design) **+ new `architecture-diagram.html`** (5 tabs: Inngest sequence flow, data model ER, which-question decision logic, close-enough flavor routing, staging/off-slot-hold). First-class `file_upload_job` (Realtime-streamed, zip→children tree via `parent_job_id`, `upload_type` TEXT + nullable `submission_version_id`) kills the "minutes of nothing"; `file_upload_decision` (4 `question_type`s incl. `exact_duplicate` sha256 pre-check → component registry, race-safe answer, **no timeout**) asks per file to **version / replace / separate / discard / promote / keep**. Detection = flavor-routed (text-Jaccard for docs, reused `sharp` `computeSheetSimilarity`/`matchSheets` for sheets), argmax one-decision-per-file, off-slot hold (attach-only-on-resolve). Grounded in the 2026-08-24 123 MB zip trace. Open: Q-detection tuning, Q-exactdup, Q-planset-trace, Q-RLS. | `workspaces/pre-processing-v2/file-upload-zip-loading-and-hitl-questions/DESIGN-SPEC.md` | 2026-08-25 | 2026-08-25 (#240) |
| 15 | Pre-Processing v2 **bounding-box second-pass tightening** DESIGN SPEC (Draft v1): new post-reconcile `box-refine` step in the preprocessing runbook that corrects Reader-A's eyeballed boxes. Per block, a **script** renders a padded crop + a second crop with the current box drawn as a **translucent blue overlay**; the **agent** (vision, two images) returns a tighter box **in crop coords**; a **script** remaps crop→sheet 0–1000. Iterates up to **N=3** passes (re-crop each time). Coordinates only — no merge/remove (stays with reconciler), no crop-grouping (v2). Upstream of `normalize.ts`, so persisted shape + IG parity oracle unchanged. Q1–Q9 open (loop termination, padding, DPI, which blocks, tolerance, cost). | `workspaces/pre-processing-v2/bounding-box-second-pass-tightening/design-spec.md` | 2026-08-19 | 2026-08-19 |