# Case 015 — crc-DE:DE-31 (Specific viewport cutoff fixed, but significant callout overlap persists on east)

**Agent status:** `uncertain` (confidence `medium`) — **Triage:** none (no-triage mode)
**Pattern tags:** `any-vs-most-quantifier`, `dispute-resolved-side-not-audited`

## City MCR comment (as restated by agent)
(no `comment` field in consolidated-findings output; reference guide checklist item below)

## Agent observation (verbatim)
> Reviewed Sheet 25 via vision analysis with the U0 reference figure showing the original viewport cutoff/redundant callout issue. Vision finding: The specific viewport cutoff shown in the reference figure (partial callouts for 'XISTING', '8" HDPE', '4" HDPE', 'RIM: 545') has been resolved — the callout box for 'EXISTING 4' DIA. STORM MANHOLE RIM: 545.48' is now fully visible. However, 'general visual clutter remains high, particularly on the eastern edge where callout boxes overlap underlying linework and each other.' The eastern corridor where SD-04 and SD-05 converge with water, wastewater, and electric lines remains congested with overlapping text.

## Agent reasoning (verbatim)
> The requirement states plans must be 'free of redundant callouts and viewport cutoffs, so the intended design is clearly interpretable across all sheets.' While the specific viewport cutoff from the U0 reference figure was corrected, the vision analysis confirms ongoing callout overlap that prevents clear interpretability. This is a recommendation-severity item, but the standard ('clearly interpretable') has not been fully met. The congestion persists specifically in the SD-04/SD-05 area that was the subject of DE-23 as well.

## Checklist item DE-31 (verbatim from guide)
> The resubmitted plans are free of redundant callouts and viewport cutoffs, so the intended design is clearly interpretable across all sheets.

## Human triage note
> no triage available

## Hypothesis — why the original tag was misapplied

**Corrected interpretation (2026-07-01).** The consolidated verdict is `uncertain` (resolved=2 failed=3), which the cross-run consolidator produced by design because per-run agents disagreed — not because any single agent hedged. The `winningFinding.reasoning` quoted above comes from the failed-side per-run agent (tentativeStatus=`failed`), whose vote was CORRECT on this calibration item. The real failure lives in the resolved-side per-run findings for this ref, which this audit did not systematically examine. Root-causing this case requires reading those per-run agents' reasoning (available in `_run_artifacts/output/consolidated-findings.json` under `perRunFindings[]` filtered to `status=="resolved"`).

## Remediation hypothesis

1. **Investigation:** audit the resolved-side per-run findings for this ref and classify their failure patterns using the standard taxonomy.
2. **Where:** run a follow-up Agent-4-style pass with prompt scope extended to include losing-side per-run findings, not only `winningFinding`.
3. **Maps to:** R-05 (audit resolved-side per-run findings for disputed items).
