# Case 011 — crc-DE:DE-23 (SD-04 and SD-05 plan views remain congested with overlapping callouts; legibilit)

**Agent status:** `uncertain` (confidence `medium`) — **Triage:** none (no-triage mode)
**Pattern tags:** `dispute-resolved-side-not-audited`

## City MCR comment (as restated by agent)
(no `comment` field in consolidated-findings output; reference guide checklist item below)

## Agent observation (verbatim)
> Reviewed Sheet 25 (Storm Drain Plan) via vision analysis. The storm drain lines for SD-04 and SD-05 use bold, thick line weights. However, the plan view is heavily congested, particularly in the eastern corridor where multiple utility layers (water '-W-', wastewater '-WW-', underground electric '-UGE-', telecom) converge. Callout boxes for SD-05 structures (STA 2+09.34, 2+11.20, 2+44.59) overlap each other and underlying linework. The guide.md for Sheet 25 confirms the sheet is '98% similar' to U0, with changes limited to title block updates and one new SD-03 callout — the SD-04/SD-05 plan view layout was not reworked.

## Agent reasoning (verbatim)
> The requirement is that SD-04 and SD-05 plan views are 'cleaned up and layer-controlled so that the proposed storm lines are clearly visible and legible.' Vision analysis confirmed that overlapping callout boxes and utility congestion make it 'difficult to cleanly read the text and visually track the utility layers without confusion.' Since the U1 sheet is 98% similar to U0 with no substantive cleanup of the SD-04/SD-05 plan view area, the legibility issue persists.

## Checklist item DE-23 (verbatim from guide)
> The storm drain layout sheets, specifically the plan view for SD-04 and SD-05, are cleaned up and layer-controlled so that the proposed storm lines are clearly visible and legible.

## Human triage note
> no triage available

## Hypothesis — why the original tag was misapplied

**Corrected interpretation (2026-07-01).** The consolidated verdict is `uncertain` (resolved=2 failed=3), which the cross-run consolidator produced by design because per-run agents disagreed — not because any single agent hedged. The `winningFinding.reasoning` quoted above comes from the failed-side per-run agent (tentativeStatus=`failed`), whose vote was CORRECT on this calibration item. The real failure lives in the resolved-side per-run findings for this ref, which this audit did not systematically examine. Root-causing this case requires reading those per-run agents' reasoning (available in `_run_artifacts/output/consolidated-findings.json` under `perRunFindings[]` filtered to `status=="resolved"`).

## Remediation hypothesis

1. **Investigation:** audit the resolved-side per-run findings for this ref and classify their failure patterns using the standard taxonomy.
2. **Where:** run a follow-up Agent-4-style pass with prompt scope extended to include losing-side per-run findings, not only `winningFinding`.
3. **Maps to:** R-05 (audit resolved-side per-run findings for disputed items).
